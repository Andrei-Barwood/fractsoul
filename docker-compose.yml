services:
  ingest-api:
    build:
      context: ./backend/services/ingest-api
      dockerfile: Dockerfile
    container_name: fractsoul-ingest-api
    environment:
      APP_PORT: "8080"
      GIN_MODE: release
      LOG_LEVEL: info
      NATS_URL: nats://nats:4222
      TELEMETRY_SUBJECT: telemetry.raw.v1
      DATABASE_URL: postgres://postgres:postgres@timescaledb:5432/mining?sslmode=disable
      TELEMETRY_PROCESSOR_ENABLED: "true"
    ports:
      - "8080:8080"
    depends_on:
      timescaledb:
        condition: service_healthy
      nats:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  timescaledb:
    image: timescale/timescaledb:2.15.3-pg16
    container_name: fractsoul-timescaledb
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mining
    ports:
      - "5432:5432"
    volumes:
      - timescaledb-data:/var/lib/postgresql/data
      - ./infra/docker/timescaledb/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d mining"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  nats:
    image: nats:2.10-alpine
    container_name: fractsoul-nats
    command: ["-js", "-m", "8222"]
    ports:
      - "4222:4222"
      - "8222:8222"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8222/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  timescaledb-data:
